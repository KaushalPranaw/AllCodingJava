/*
package CoreJava.ClassLoader;

import java.io.File;
import java.net.URL;

public class TestCustomClassLoader {
    public static void main(String[] args) {
        // Update with the relative or absolute path based on where the YAML file is
        //YmlReader reader = new YmlReader("C:\\Users\\pranaw_kaushal\\Documents\\ALL_EPAM_TRAINING\\core-java-training\\basic-concepts\\src\\CoreJava.ClassLoader\\example.yml");

        try {
            // Directory where compiled class files are stored
            File classDir = new File("path/to/classes");
            URL[] classLoaderUrls = new URL[]{classDir.toURI().toURL()};

        }
    }
}
*/
